version: "3.9"

services:
  backend:
    build: ./backend
    container_name: mlops_backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: mlops_frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
